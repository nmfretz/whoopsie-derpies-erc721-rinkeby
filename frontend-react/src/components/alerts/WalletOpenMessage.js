const WalletOpenMessage = () => {
  return (
    <div className="notification is-primary is-light container mt-5 custom-smaller-mobile-text">
      <span>Wallet is open and waiting on user to confirm transaction...</span>
    </div>
  );
};

export default WalletOpenMessage;
